{{ $redis_master_replicas := getNumber "rc/redis-master/number" }}
{{ $container_name        := getString "rc/redis-master/name" }}
{{ $container_image       := getString "rc/redis-master/image" }}
{{ $container_version     := getString "rc/redis-master/version" }}
{{ $container_port        := getNumber "rc/redis-master/port" }}

apiVersion: v1
kind: ReplicationController
metadata:
  name: redis-master
  labels:
    name: redis-master
spec:
  replicas: {{ $redis_master_replicas }}
  selector:
    name: redis-master
  template:
    metadata:
      labels:
        name: redis-master
    spec:
      containers:
      - name: {{ $container_name }}
        image: {{ $container_image }}:{{ $container_version }}
        ports:
        - containerPort: {{ $container_port }}
